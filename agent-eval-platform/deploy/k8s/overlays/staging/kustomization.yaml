apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target: { kind: Deployment, name: server }
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target: { kind: Deployment, name: web }
# CI 更新镜像 digest（书 48.5 GitOps）：
# images:
#   - name: eval-server:dev
#     newName: ghcr.io/yourorg/eval-server
#     digest: sha256:...
